// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    projectId: 'manageofficespace',
    appId: '1:519251109235:web:18481e8af1619dd8e65106',
    storageBucket: 'manageofficespace.appspot.com',
    apiKey: 'AIzaSyDOs20_d46-pFzsHKbtxEbLPLaGHWevIuE',
    authDomain: 'manageofficespace.firebaseapp.com',
    messagingSenderId: '519251109235',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
